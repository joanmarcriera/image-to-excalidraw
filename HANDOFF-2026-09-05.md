# Handoff — 2026-09-05

## What changed
- Added `CLAUDE.md` (purpose, quick start, layout, conventions, testing, gotchas, deployment).
- `README.md` left unchanged — already covers what/why/quick-start/provider setup/notes.

## Verified against the actual code
- `npm test` passes (13/13) as-is: `node --test` covers ai-client, excalidraw, scene-refiner,
  scene-schema, and svg-scene.
- Fixed a real inaccuracy in the first draft: CLAUDE.md attributed "local SVG-to-scene conversion"
  to `lib/svg-preview.mjs`. That file actually does the *opposite* direction — `renderSceneSvg()`
  turns a scene graph back into an SVG preview. The real upload-bypass conversion
  (`extractSceneFromSvg()`) lives in `public/svg-scene.js`, which wasn't listed under `public/` at
  all. Corrected the Layout section and the SVG-bypass gotcha to name the right file/direction.
- `server.mjs` PORT/HOST env var defaults (3000 / 127.0.0.1) confirmed by reading the source.
- CNAME confirmed as `i2e.riera.co.uk`, matching the Deployment section.

## Left to do
- Pre-existing, out-of-scope: README.md has a leftover "Why there are no extra MCP servers or
  vendored skills here" section from an earlier drafting pass — reads oddly for a public README but
  predates this task and wasn't part of today's docs work, so left untouched. Worth a cleanup pass
  separately.
- Docker Compose / GitHub Pages deploy paths documented but not executed in this pass (per the
  drafting agent's note) — local `npm test` was run and passes.
